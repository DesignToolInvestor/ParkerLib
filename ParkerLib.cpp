/*
  P a r k e r L i b . c p p

  This file is required by the VS (Visual Studio) project type, but is not used because this is 
  really a library project.  But the command line project type was used because I hadn't yet made
  any of the library type files work for what are essentially just template header files.
*/

#include <iostream>

int main()
{
  std::cout << "Hello World!\n";
}
